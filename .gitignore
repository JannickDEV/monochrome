node_modules
dist
.DS_Store
*.local
.vite
.claude
# Docker
.env
# bot: rotated Spotify first-party refresh token (persisted across restarts)
.spotify-fp-refresh-token
# Neutralino
.tmp/
.vitest-attachments/
**/__screenshots__/*
bin/
*.log
.storage/
auth_storage/
www
neutralino.js
package-lock.json
# Direnv
.envrc
# vim
.lazy.lua
Caddyfile
dev-dist
bun.lock
load-balancer
# We already have bun.lock, bun.lockb is redundant
bun.lockb
.wrangler
.opencode
